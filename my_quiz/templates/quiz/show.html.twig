{% extends 'base.html.twig' %}

{% block title %}Hello CategorieController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


	<h1>{{ question.id}}</h1>
	<p>{{ question.question }}</p>
	<p>{{ question.idCategorie}}</p>

	{# <p>{{ reponse.reponse}}</p> #}

	{# <a href="/categorie/{{categorie.id}}" class="btn btn-dark">Show</a> #}
	{# {% if question.idcategorie == (question.idcategorie - 1)*10+1%} #}
    	
		<a href="{{ question.id + 1 }}" class="btn btn-outline-primary">next question</a><br>

    {# {% endif %} #}
	<a href="/quiz">Go Back</a>

{% endblock %}