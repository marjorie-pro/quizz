{% extends 'base.html.twig' %}

{% block title %}Hello CategorieController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/home/marjorie/rendu/MVC_my_quiz/my_quiz/src/Controller/CategorieController.php'|file_link(0) }}">src/Controller/CategorieController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/home/marjorie/rendu/MVC_my_quiz/my_quiz/templates/categorie/index.html.twig'|file_link(0) }}">templates/categorie/index.html.twig</a></code></li>
    </ul>
</div>
	<h1>Categories</h1>
	{% if categories %}
	{% for categorie in categories %}
	<div>{{ categorie.name }}</div>
	<a href="/quiz/{{categorie.id}}" class="btn btn-dark">Show</a>
	<a href="/categorie/{{categorie.id}}/{{(categorie.id - 1)*10+1}}">{{categorie.name}}</a>
	
	<a href="/question" class="btn btn-dark">Voir questions</a>
	{# <a href="{{ path('question', {'id': question.id}) }}" class="btn btn-dark">voir les questions</a> #}
	{% endfor %}
	{% else %}
	<p>No categories to display</p>
	{% endif %}
{% endblock %}
